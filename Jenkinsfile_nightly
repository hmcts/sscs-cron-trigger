#!groovy

properties([
  pipelineTriggers([cron('H 06 * * 1-5')])
])

@Library("Infrastructure@2.4.4")

def type = "java"
def product = "sscs"
def component = "cron-trigger"

withNightlyPipeline(type, product, component) {
  enableSlackNotifications('#sscs-tech')
  nonServiceApp()
}
